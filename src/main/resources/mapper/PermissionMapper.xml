<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gcc.gccmap.mapper.PermissionMapper">
    <select id="queryPermissionByUserId" resultType="String">
            select permission_name from user_role ur
            left join role r on ur.role_id = r.role_id
            left join role_permission rp on r.role_id = rp.r_id
            left join permission p on rp.p_id = p.permission_id
            where ur.u_id = #{userId}
    </select>

    <select id="queryUserType" resultType="String">
        select role_name from user_role ur left join role r on ur.role_id = r.role_id
        where u_id = #{userId}
    </select>

</mapper>